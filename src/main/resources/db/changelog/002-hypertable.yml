databaseChangeLog:
  - changeSet:
      id: create-hypertable
      author: system
      runOnChange: true
      changes:
        - sql: |
            SELECT create_hypertable('ticks', 'time', if_not_exists => TRUE);
